---
- hosts: all
  tasks:

  - name: Install all updates and reboot as many times as needed
    ansible.windows.win_updates:
      category_names: '*'
      reboot: true

  - name: Update all packages installed through Chocolatey
    win_chocolatey:
      name: all
      state: latest
...